--- origsrc/php-7.0.13/build/php.m4	2016-11-08 09:07:39.000000000 -0600
+++ src/php-7.0.13/build/php.m4	2016-11-24 17:26:46.357692100 -0600
@@ -835,7 +835,7 @@
 	\$(LIBTOOL) --mode=install cp $3/$1.$suffix \$(phplibdir)
 
 $3/$1.$suffix: \$($2) \$(translit($1,a-z_-,A-Z__)_SHARED_DEPENDENCIES)
-	\$(LIBTOOL) --mode=link ifelse($4,,[\$(CC)],[\$(CXX)]) -shared \$(COMMON_FLAGS) \$(CFLAGS_CLEAN) \$(EXTRA_CFLAGS) \$(LDFLAGS) $additional_flags -o [\$]@ -export-dynamic -avoid-version -prefer-pic -module -rpath \$(phplibdir) \$(EXTRA_LDFLAGS) \$($2) \$(translit($1,a-z_-,A-Z__)_SHARED_LIBADD)
+	\$(LIBTOOL) --mode=link ifelse($4,,[\$(CC)],[\$(CXX)]) -shared \$(COMMON_FLAGS) \$(CFLAGS_CLEAN) \$(EXTRA_CFLAGS) \$(LDFLAGS) $additional_flags -o [\$]@ -export-dynamic -avoid-version -module -no-undefined -rpath \$(phplibdir) \$(EXTRA_LDFLAGS) \$($2) ifelse($1,libphp8,,-lphp8) \$(translit($1,a-z_-,A-Z__)_SHARED_LIBADD)
 
 EOF
 ])
@@ -1685,6 +1685,10 @@
    SHLIB_SUFFIX_NAME=dylib
    SHLIB_DL_SUFFIX_NAME=so
    ;;
+ *cygwin*[)]
+   SHLIB_SUFFIX_NAME=dll.a
+   SHLIB_DL_SUFFIX_NAME=dll
+   ;;
  esac
 ])
 
